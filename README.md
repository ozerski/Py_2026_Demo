# Первый readme
версия __1.0__

**Описание** проекта `Demo`

26.04.2026 наконец-то подключился к git и затолкнул туда соммит с 
локального репозитория.


## Работа с git
### Локальный репозиторий 
Подключение проекта к git в терминале (обязательно из папки проекта):

- инициализация пустого репозитория (создаётся папка `.git`):
    **git init**
- просмотр текущего состояния проекта [_н/обяз._]
(<span style="color: green;">отслеживаемые `Changes to be committed`</span>, 
игнорируемые (см. .gitignore),
<span style="color: brown;">не отслеживаемые `Untracked files`</span>): 
    **git status**
- исключение из отслеживаемых (например, настройки): указываем имя 
  файла/папки в файле `.gitignore` 
- индесация для последующего закоммичивания (точка = "все", либо названия 
  папок и файлов ч/з запятую):
    **git add .**
- создание коммита: **git commit -m "комментарии к коммиту"**
- просмотр коммитов [_н/обяз._]: **git log**



### Удалённый репозиторий

Список удалённых репозиториев: **git remote -v** .\
Переключение между ветками:
- **git checkout -b** _task1_ <span style="color: gray;"># 
  создание/переключение на ветку task1</span>
- **git checkout -b main** <span style="color: gray;"># 
  создание/переключение на ветку main</span>
- **git merge** _task1_ <span style="color: gray;"># 
  слияние с веткой main</span>

титчим